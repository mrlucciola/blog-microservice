import { PORTS } from "./networking";

export const PORT_POSTS = PORTS.posts;
export const PORT_COMMENTS = PORTS.comments;
export const PORT_QUERY = PORTS.query;
export const PORT_MODERATION = PORTS.moderation;
export const PORT_EVENT_BUS = PORTS.eventBus;
